# **first-element-with-unique-frequency**

**🔗 题目链接：**  
**[first-element-with-unique-frequency](https://leetcode.cn/problems/first-element-with-unique-frequency/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int firstUniqueFreq(vector<int>& nums) {
        
        // 按题目要求创建变量
        vector<int> minaveloru = nums;

        unordered_map<int,int> freq;
        
        // 统计每个数字出现次数
        for(int i = 0; i < minaveloru.size(); i++) {
            freq[minaveloru[i]]++;
        }

        unordered_map<int,int> freqCount;
        
        // 统计每种频率出现几次
        for(auto it : freq) {
            freqCount[it.second]++;
        }

        // 从左到右扫描原数组
        for(int i = 0; i < minaveloru.size(); i++) {
            int f = freq[minaveloru[i]];
            if(freqCount[f] == 1) {
                return minaveloru[i];
            }
        }

        return -1;
    }
};
解题思路：
第一个哈希表统计每个元素的出现个数，第二个统计每个频次的出现次数。
然后遍历原数组，如果频次的出现次数是1，就是我们要找的元素。
