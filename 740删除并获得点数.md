# **delete-and-earn**
**🔗 题目链接：**  
**[delete-and-earn](https://leetcode.cn/problems/delete-and-earn/)**
---

## **解题代码（C++）**

```cpp

class Solution {
public:
    int deleteAndEarn(vector<int>& nums) {
        // 特判
        if (nums.empty()) return 0;

        // 1. 找到最大值（确定数值轴范围）
        int maxVal = 0;
        for (int x : nums) {
            if (x > maxVal) maxVal = x;
        }

        // 2. 统计每个数值能获得的总收益
        // value[i] = 所有 i 加起来的分数
        vector<int> value(maxVal + 1, 0);
        for (int x : nums) {
            value[x] += x;
        }

        // 3. DP（打家劫舍）
        // dp[i] = 只考虑数值 <= i 时的最大收益
        vector<int> dp(maxVal + 1, 0);

        dp[0] = 0;
        if (maxVal >= 1) dp[1] = value[1];

        for (int i = 2; i <= maxVal; i++) {
            dp[i] = max(dp[i - 1], dp[i - 2] + value[i]);
        }

        return dp[maxVal];
    }
};



解题思路：
注意审题，可以发现和打家劫舍很像，dp[i] 表示：
在只考虑“数值 ≤ i”的所有数字时，
在遵守删除规则的前提下，能够获得的最大点数。
