# **special-binary-string**
**🔗 题目链接：**  
**[special-binary-string](https://leetcode.cn/problems/special-binary-string/)**
---
## **解题代码（C++）**

```cpp
class Solution {
public:
    string makeLargestSpecial(string s) {
        vector<string> blocks;
        int count = 0;
        int start = 0;

        // 拆分成最外层特殊子串
        for (int i = 0; i < s.size(); i++) {
            if (s[i] == '1')
                count++;
            else
                count--;

            // 找到一个完整块
            if (count == 0) {
                // 去掉首尾 1 和 0
                string inner = s.substr(start + 1, i - start - 1);

                // 递归处理内部
                string processed = makeLargestSpecial(inner);

                // 拼回 1 + 内部 + 0
                blocks.push_back("1" + processed + "0");

                start = i + 1;
            }
        }

        // 按字典序降序排列
        sort(blocks.rbegin(), blocks.rend());

        // 拼接所有块
        string result;
        for (auto &b : blocks)
            result += b;

        return result;
    }
};

解题思路：
首先，是递归分割将字符串分为特殊子串，然后递归继续分割，然后返回这一层倒序排列拼接，
因为是相邻的交换，所以所有排列都是可以实现的，然后一直返货到第一层拼接成result提交
结果。注意是把1，0之间的子串进行操作，整道题目就是括号匹配，1，0之间依然是特殊的子
串。
