**#kth-smallest-element-in-a-sorted-matrix**

**🔗 题目链接：**  
**[kth-smallest-element-in-a-sorted-matrix](https://leetcode.cn/problems/kth-smallest-element-in-a-sorted-matrix/)**

---

## 

```cpp
class Solution {
public:
    int kthSmallest(vector<vector<int>>& matrix, int k) {
        int n = matrix.size();

        int left = matrix[0][0];
        int right = matrix[n - 1][n - 1];

        while (left < right) {
            int mid = left + (right - left) / 2;

            if (countLessEqual(matrix, mid) >= k)
                right = mid;// mid 有可能是答案 → 收缩右边界
            else
                left = mid + 1;// mid 不行 → 排除 mid
        }
        return left;
    }

    int countLessEqual(vector<vector<int>>& matrix, int mid) {
        int n = matrix.size();
        int i = n - 1, j = 0;
        int cnt = 0;

        while (i >= 0 && j < n) {
            if (matrix[i][j] <= mid) {
                cnt += i + 1;
                j++;
            } else {
                i--;
            }
        }
        return cnt;
    }
};

解题思路：
这道题目采用二分法的思路就是判断比那个数小的是不是k个，如果是就是找到了，否则继续按照值二分，根据题目，mid 每次不一定是数组里的数
但是最终答案 left 会落在数组里，这就是题目特性保证的。mid 只是二分过程中的“猜测值”，可能不存在于矩阵里。left 收敛到的值，是整个区间内满足第 k 小条件的最小可行值。
由于题目保证第 k 小元素一定存在且唯一，所以最终 left 一定指向矩阵里实际的这个数。
