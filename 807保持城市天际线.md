# **max-increase-to-keep-city-skyline**
**🔗 题目链接：**  
**[max-increase-to-keep-city-skyline](https://leetcode.cn/problems/max-increase-to-keep-city-skyline/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int maxIncreaseKeepingSkyline(vector<vector<int>>& grid) {
     int n=grid.size();
     vector<int>temp1(n,0);
     vector<int>temp2(n,0);
     for(int i=0;i<n;i++)
     {
        for(int j=0;j<n;j++)
        {
            temp1[i]=max(grid[i][j],temp1[i]);
            temp2[i]=max(grid[j][i],temp2[i]);
        }
     }
     vector<int>temp(n,0);
     for(int i=0;i<n;i++)
     {
       temp[i]=min(temp1[i],temp2[i]);
     }
     int result=0;
        for(int i = 0; i < n; i++) {
            for(int j = 0; j < n; j++) {
                result += min(temp1[i], temp2[j]) - grid[i][j]; // 注意这里用 temp2[j] 而不是 temp2[i]
            }
        }
      return result;
     
    }
};
解题思路：
整体而言就是一道最简单的贪心题目，同时要注意遍历的时候对行列的更新，一开始我错误的使用temp去取temp1[i]和temp2[i]
的最小值，这是不可取的，因为这只有在对角线上是满足的。
