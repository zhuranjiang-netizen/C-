# **most-profit-assigning-work**
**🔗 题目链接：**  
**[most-profit-assigning-work](https://leetcode.cn/problems/most-profit-assigning-work/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int maxProfitAssignment(vector<int>& difficulty,
                            vector<int>& profit,
                            vector<int>& worker) 
    {
        int n = difficulty.size();
        // Step 1: 建立工作数组并按难度排序
        vector<pair<int,int>> jobs(n);
        for (int i = 0; i < n; ++i)
            jobs[i] = {difficulty[i], profit[i]};

        sort(jobs.begin(), jobs.end()); // 按 difficulty 升序排序

        // Step 2: 预处理前缀最大 profit
        int best = 0;
        for (int i = 0; i < n; ++i)
        {
            best = max(best, jobs[i].second);
            jobs[i].second = best; // 当前难度及之前的最大 profit
        }

        // Step 3: 将工人按能力排序
        sort(worker.begin(), worker.end());

        // Step 4: 双指针扫描
        int ans = 0;
        int j = 0; // jobs 指针
        int m = worker.size();
        best = 0; // 当前能拿到的最大 profit
        for (int i = 0; i < m; ++i)
        {
            int w = worker[i];
            // 扫描所有难度 ≤ w 的工作
            while (j < n && jobs[j].first <= w)
            {
                best = jobs[j].second; // 已经是前缀最大 profit
                j++;
            }
            ans += best; // 给这个工人选择最大 profit
        }

        return ans;
    }
};

        

解题思路：
贪心的思路就是根据每个工人的能力按照工人的能力选利润最大的，预处理就是预处理就是把当前难度下的最大利润算出来来简化后序操作。

