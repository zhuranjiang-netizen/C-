# **can-i-win**
**🔗 题目链接：**  
**[can-i-win](https://leetcode.cn/problems/can-i-win//)**
---
https://leetcode.cn/problems/can-i-win/
## **解题代码（C++）**

```cpp
class Solution {
public:
    int n, target;
    vector<int> memo;  // -1 未算，0 输，1 赢

    bool dfs(int used, int sum) {
        // 如果算过，直接返回
        if (memo[used] != -1)
            return memo[used];

        // 尝试选择每一个没用过的数
        for (int i = 1; i <= n; i++) {
            int bit = 1 << (i - 1);
            if (used & bit) continue;  // 已经用过

            // ① 我这一步直接赢
            if (sum + i >= target) {
                memo[used] = 1;
                return true;
            }

            // ② 让对手进入必败状态
            if (!dfs(used | bit, sum + i)) {
                memo[used] = 1;
                return true;
            }
        }

        // 所有选择都会让对手赢 → 我必败
        memo[used] = 0;
        return false;
    }

    bool canIWin(int maxChoosableInteger, int desiredTotal) {
        n = maxChoosableInteger;
        target = desiredTotal;

        // 特判：所有数加起来都不够
        int sum = n * (n + 1) / 2;
        if (sum < target) return false;

        memo.assign(1 << n, -1);
        return dfs(0, 0);
    }
};

解题思路：
注意题目，两个人的和是公用的，看谁先到目标，同时采用二进制的形式去表示每个选取状态以提高速率，
每层的逻辑依然不管哪个对手，只是负责当前进行选择的对手，同时用一个容器去记录可能。
