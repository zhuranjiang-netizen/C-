# **binary-gap**
**🔗 题目链接：**  
**[binary-gap](https://leetcode.cn/problems/binary-gap/)**
---
## **解题代码（C++）**

```cpp
class Solution {
public:
    int binaryGap(int n) {
        vector<int> nums;

        // 把 n 的值先保存，避免后面被除没
        int temp = n;

        while(temp != 0)
        {
            int m = temp % 2;
            nums.push_back(m);
            temp = temp / 2;
        }

        int last = -1;   // 记录上一个1的位置
        int ans = 0;     // 记录最大距离

        for(int i = 0; i < nums.size(); i++)
        {
            if(nums[i] == 1)
            {
                if(last != -1)
                {
                    ans = max(ans, i - last);
                }
                last = i;
            }
        }

        return ans;
    }
};
解题思路：
注意一定是讨论的相邻的。
