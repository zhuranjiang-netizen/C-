# **Majority Element**

**🔗 题目链接：**  
**[majority-element](https://leetcode.cn/problems/majority-element/)**

---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int sortelement(vector<int>& nums, int left, int right) {
        // 1️⃣ 递归出口
        if (left == right) {
            return nums[left];
        }

        int mid = (left + right) / 2;

        // 2️⃣ 分
        int leftnum  = sortelement(nums, left, mid);
        int rightnum = sortelement(nums, mid + 1, right);

        // 3️⃣ 治
        if (leftnum == rightnum) {
            return leftnum;
        }

        // 4️⃣ 合并：在整个区间统计
        int countLeft = 0, countRight = 0;
        for (int i = left; i <= right; i++) {
            if (nums[i] == leftnum)  countLeft++;
            if (nums[i] == rightnum) countRight++;
        }

        return countLeft > countRight ? leftnum : rightnum;
    }

    int majorityElement(vector<int>& nums) {
        return sortelement(nums, 0, nums.size() - 1);
    }
};
解题思路：

由于题目保证必定存在一个主元素，因此这个元素必定在某一半边占据主导数量。

采用分治法，每次将数组划分为 (left, mid) 和 (mid+1, right) 两部分进行递归。

注意递归终止条件是 left == right，这样每个元素仅被访问一次且不会越界。

合并阶段对左右两部分返回的候选元素进行计数比较，最终返回数量更多的那个元素，即整个区间的主元素。
