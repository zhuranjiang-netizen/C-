# **sum-of-root-to-leaf-binary-numbers**
**🔗 题目链接：**  
**[sum-of-root-to-leaf-binary-numbers](https://leetcode.cn/problems/sum-of-root-to-leaf-binary-numbers/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int backtracking(TreeNode* root, int cur)
    {
        if(root == nullptr)
            return 0;

        // 更新当前路径的二进制值
        cur = cur * 2 + root->val;

        // 如果是叶子节点
        if(root->left == nullptr && root->right == nullptr)
            return cur;

        return backtracking(root->left, cur) +
               backtracking(root->right, cur);
    }

    int sumRootToLeaf(TreeNode* root) {
        return backtracking(root, 0);
    }
};
解题思路：
注意二进制是倒着算的。
