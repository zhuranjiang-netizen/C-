# **minimum-number-of-days-to-make-m-bouquets**
**🔗 题目链接：**  
**[minimum-number-of-days-to-make-m-bouquets](https://leetcode.cn/problems/minimum-number-of-days-to-make-m-bouquets/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    bool canMake(int day, vector<int>& bloomDay, int m, int k) {
        int cnt = 0;     // 连续开的花
        int bouquets = 0;

        for (int x : bloomDay) {
            if (x <= day) {
                cnt++;
                if (cnt == k) {
                    bouquets++;
                    cnt = 0;
                }
            } else {
                cnt = 0;
            }
        }
        return bouquets >= m;
    }

    int minDays(vector<int>& bloomDay, int m, int k) {
        long long n = bloomDay.size();
        if ((long long)m * k > n) return -1;

        int left = *min_element(bloomDay.begin(), bloomDay.end());
        int right = *max_element(bloomDay.begin(), bloomDay.end());

        while (left < right) {
            int mid = left + (right - left) / 2;
            if (canMake(mid, bloomDay, m, k)) {
                right = mid;
            } else {
                left = mid + 1;
            }
        }
        return left;
    }
};


解题思路：
数组中有最小天数和最大天数，随着天数增加，可制作花束也会增加，因此可以对天数做二分法，
并通过辅助函数判断mid那天可制作的花束是否满足，来缩小范围。
