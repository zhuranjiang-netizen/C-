# **house-robber-ii**
**🔗 题目链接：**  
**[house-robber-ii](https://leetcode.cn/problems/house-robber-ii/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int rob(vector<int>& nums) {
        int n = nums.size();
        if (n == 1) return nums[0];
        if (n == 2) return max(nums[0], nums[1]);

        // 情况1：偷 [0..n-2]
        int case1 = robLinear(nums, 0, n - 2);

        // 情况2：偷 [1..n-1]
        int case2 = robLinear(nums, 1, n - 1);

        return max(case1, case2);
    }

private:
    // 线性数组的 House Robber I
    int robLinear(vector<int>& nums, int start, int end) {
        int prev = 0;  // dp[i-2]
        int curr = 0;  // dp[i-1]

        for (int i = start; i <= end; i++) {
            int temp = max(curr, prev + nums[i]);
            prev = curr;
            curr = temp;
        }

        return curr;
    }
};
解题思路：
根据题目环形的结构可分析出，由于这个环形使得题目复杂化，但是进一步分析不就是偷第一家和不偷两种情况吗。
因此采用了分类讨论之后再比较的方法使得题目就转换为最普通的动态规划了，由于只需要考虑前两个元素的情况，
因此连额外的空间都不用去开辟了。
