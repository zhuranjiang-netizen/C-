# **maximum-erasure-value**
**🔗 题目链接：**  
**[maximum-erasure-value](https://leetcode.cn/problems/maximum-erasure-value/)**
---
## **解题代码（C++）**

```cpp

class Solution {
public:
    int maximumUniqueSubarray(vector<int>& nums) {
        int n = nums.size();
        unordered_set<int> hash;
        int l = 0, maxsum = 0, result = 0;

        for (int r = 0; r < n; r++) {
            // 如果 nums[r] 已经在窗口中，缩小左边界
            while (hash.count(nums[r])) {
                maxsum -= nums[l];
                hash.erase(nums[l]);
                l++;
            }
            // 添加当前元素
            hash.insert(nums[r]);
            maxsum += nums[r];

            // 更新结果
            result = max(result, maxsum);
        }

        return result;
    }
};

解题思路：
采用滑动窗口与哈希表结合的方法。
