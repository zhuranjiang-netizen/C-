# **find-kth-bit-in-nth-binary-string**
**🔗 题目链接：**  
**[find-kth-bit-in-nth-binary-string](https://leetcode.cn/problems/find-kth-bit-in-nth-binary-string/)**
---
## **解题代码（C++）

```cpp
class Solution {
public:
    int maxSumAfterPartitioning(vector<int>& arr, int k) {
        int n = arr.size();
        
        vector<int> dp(n + 1, 0);  // dp[0] = 0
        
        for(int i = 1; i <= n; i++)
        {
            int maxnum = 0;
            
            for(int len = 1; len <= k && i - len >= 0; len++)
            {
                maxnum = max(maxnum, arr[i - len]);
                dp[i] = max(dp[i], dp[i - len] + len * maxnum);
            }
        }
        
        return dp[n];
    }
};

解题思路：
推演然后找到规律，发现左边和右边是有关系的，且中间的数字一定是1，如果k大于一半，右边的可以转到左边来算
采用递归的思路。
