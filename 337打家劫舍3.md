# **house-robber-iii**
**🔗 题目链接：**  
**[house-robber-iii](https://leetcode.cn/problems/house-robber-iii/)**
---

## **解题代码（C++）**

```cpp
class Solution {
public:
    // 返回值：pair<不偷当前节点的最大值, 偷当前节点的最大值>
    pair<int, int> dfs(TreeNode* root) {
        if (root == nullptr) {
            return {0, 0};
        }

        // 先算左右子树（后序遍历）
        auto left = dfs(root->left);
        auto right = dfs(root->right);

        // 不偷当前节点：左右子树可以偷也可以不偷，取最大
        int notRob = max(left.first, left.second)
                   + max(right.first, right.second);

        // 偷当前节点：左右子树都不能偷
        int rob = root->val + left.first + right.first;

        return {notRob, rob};
    }

    int rob(TreeNode* root) {
        auto res = dfs(root);
        // 根节点没有父节点约束，直接取最大
        return max(res.first, res.second);
    }
};
解题思路：
每一个节点考虑的是取当前和不取当前节点值的情况，属于树状dp的情况,确定了遍历顺序后，剩下的交给了递归。

