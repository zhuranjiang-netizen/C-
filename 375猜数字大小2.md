# **guess-number-higher-or-lower-ii**

**🔗 题目链接：**  
**[guess-number-higher-or-lower-ii](https://leetcode.cn/problems/guess-number-higher-or-lower-ii/)**

---

## **解题代码（C++）**

```cpp
class Solution {
public:
    int getMoneyAmount(int n) {
        // dp[l][r] 表示猜数字区间 [l,r] 的最小花费
        // 多开一行一列，方便处理边界情况
        vector<vector<int>> dp(n + 2, vector<int>(n + 2, 0));

        // len 是区间长度
        for (int len = 2; len <= n; len++) {
            // l 是区间左端点
            for (int l = 1; l + len - 1 <= n; l++) {
                int r = l + len - 1;
                dp[l][r] = INT_MAX;

                // j 是第一次猜的数字
                for (int j = l; j <= r; j++) {
                    int cost = j + max(dp[l][j - 1], dp[j + 1][r]);
                    dp[l][r] = min(dp[l][r], cost);
                }
            }
        }

        return dp[1][n];
    }
};

解题思路：
max：计算“如果我猜 j，最坏情况下还要花多少钱”，这是对手安排最糟的抽象
min：在所有第一次猜的 j 里，挑一个最优的策略，让自己在最坏情况下花费最少
