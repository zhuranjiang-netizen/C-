# **all-possible-full-binary-trees**
**🔗 题目链接：**
**[all-possible-full-binary-trees](https://leetcode.cn/problems/all-possible-full-binary-trees/)**
---
## **解题代码（C++）**
```cpp
class Solution {
public:
    vector<vector<TreeNode*>> memo;

    vector<TreeNode*> dfs(int n) {
        // 如果已经算过，直接返回
        if (!memo[n].empty()) {
            return memo[n];
        }

        vector<TreeNode*> result;

        // base case：只有一个节点
        if (n == 1) {
            result.push_back(new TreeNode(0));
            memo[n] = result;
            return result;
        }

        // 枚举左子树节点个数（必须是奇数）
        for (int left = 1; left < n; left += 2) {
            int right = n - 1 - left;

            vector<TreeNode*> leftTrees = dfs(left);
            vector<TreeNode*> rightTrees = dfs(right);

            // 组合左右子树
            for (TreeNode* l : leftTrees) {
                for (TreeNode* r : rightTrees) {
                    TreeNode* root = new TreeNode(0);
                    root->left = l;
                    root->right = r;
                    result.push_back(root);
                }
            }
        }

        memo[n] = result;
        return result;
    }

    vector<TreeNode*> allPossibleFBT(int n) {
        if (n % 2 == 0) {
            return {};
        }

        memo.resize(n + 1);
        return dfs(n);
    }
};

解题思路：
属于动态规划中的记忆化搜索，在递归中判断是否更新过就直接判断是不是空的，另外初始是默认空的，
在递归中要额外处理1这种情况，另外只有奇数个节点才能满足条件，在讨论每一个节点的时候要采用枚
举的方法。
